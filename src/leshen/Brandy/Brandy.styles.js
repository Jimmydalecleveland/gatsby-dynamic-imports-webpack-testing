import styled from '@emotion/styled'

import Typography from '../Typography'

// This is being exported as a styled component so that
// it can be targeted by other styled components (eg. Stack)
export const Brandy = styled(Typography)``
